# Claude Code 착수 프롬프트

## 준비

1. 폴더를 하나 만든다. 예: `Desktop\VibeCoding\instore-media-admin`
2. 그 폴더에 아래 3개 파일을 넣는다.
   - `instore-media-prototype.jsx`
   - `점내홍보매체-구축사양서.md`
   - `project_reference.txt` (기존 배너 시스템 참조 코드)
3. 그 폴더에서 Claude Code를 실행하고, 아래 `---` 아래 내용을 전부 복사해 붙여넣는다.

---

여주 프리미엄 아울렛의 **점내 홍보매체 관리 시스템**을 구축한다. 내부 직원 전용이다.

폴더에 있는 파일 3개를 먼저 읽어라.

- `점내홍보매체-구축사양서.md` — **이 문서가 기준이다.** 데이터 모델, 파생 상태 규칙, RLS, 이미지 처리, Slack 알람, 마일스톤이 모두 여기 있다.
- `instore-media-prototype.jsx` — 확정된 UI·상호작용·비즈니스 로직의 기준 구현이다. 화면 구성과 동작은 이걸 따른다.
- `project_reference.txt` — 기존 가로등 배너 시스템(`kekeke9628/banner-admin`)의 코드다. **재사용 대상이지 복사 대상이 아니다.** 사양서 2장에 무엇을 가져오고 무엇을 버릴지 표로 정리돼 있으니 반드시 그 표를 따를 것.

## 특히 주의할 것

1. **비용·승인 개념이 없다.** 참조 코드의 `UNIT`, `amountOf`, `won`, `unit_price`, `applications` 테이블, 승인/반려 큐, 공개 신청 폼, `banner_availability` 공개 뷰는 전부 만들지 않는다.
2. **공개 접근이 없다.** 모든 테이블에서 `anon`을 차단한다.
3. **개인정보를 관리하지 않는다.** 담당자·제작처 필드는 데이터 모델에도 화면에도 두지 않는다. 게시물이 갖는 정보는 업체명(필수)과 내용(선택, 비어 있으면 업체명과 동일하게 표시)뿐이다. 이 원칙은 예외 없이 전 화면·전 알람에 적용한다.
4. **핀은 지점(spot) 단위다.** 배너 시스템은 1핀 = 1배너였지만 여기는 1지점에 매체가 여러 개다.
5. **구역은 EAST LOW / EAST HIGH / WEST LOW / WEST MIDDLE / WEST HIGH 다섯 개로 고정한다.** 다른 구역명을 임의로 만들지 마라.
6. **상태를 컬럼에 저장하지 마라.** `live`/`open`(미정)/`upcoming`/`overdue`(만료)/`removed`는 날짜에서 계산한다.
7. **`removed_at`이 이 시스템의 핵심이다.**
8. **매체 유형과 개수, 면수는 데이터다.** 실제 유형은 디렉토리(1면) / 고정형 웨더워리어(2면) / 이동형 웨더워리어(2면) / 듀라트란스(1면) / 패브릭홀더(1면) 5종이지만 계속 바뀐다. `media_types` 테이블에 아이콘(`glyph`)·규격·면수를 모두 두고, 관리자가 화면에서 유형을 추가·수정(아이콘 변경 포함)·보관할 수 있게 하라. 매체를 추가할 때는 개수와 면수를 함께 입력받아 확인시킨다. 게시 이력이 있는 매체는 삭제 대신 `active=false` 보관이다.
9. **종료일은 nullable이다.** `end_date = null` = **미정** 상태이며 철거 알람 대상이 아니다. "상시"가 아니라 "시점 미정"이다 — 경과일은 계속 세서 화면에 보여주고, 365일을 넘으면 주간 요약에만 장기 미정 건수로 집계한다.
10. **이동형 매체는 지점이 고정되지 않는다.** `media_types.movable = true`인 유형은 `spot_id`를 화면에서 바꿀 수 있어야 한다.
11. **지류(인쇄물) 매체라는 물리 제약이 설계를 결정한다.** (가) 같은 매체에 기간이 겹치는 게시물은 존재할 수 없다 — `btree_gist` exclusion 제약으로 강제하라. (나) 후속 게시물이 시작됐다면 이전 것은 반드시 철거된 것이므로 기록이 비어 있으면 시스템이 자동으로 채운다. 그 결과 **사람에게 알리는 지연은 "만료" 한 종류뿐**이다.
12. **게시물 등록은 기본적으로 겹치지 않게 세팅된다.** 매체를 고르면 마지막 게시물 종료일 다음 날을 시작일로 자동 채운다. 사용자가 겹치게 바꾸면 제출 시 "겹치는 게시물이 있습니다. 그대로 진행하시겠습니까?"를 물어보고, 진행하면 **기존 게시물의 종료일을 새 게시물 시작일 하루 전으로 자동 조정**한 뒤 등록한다.
13. **이력 공백은 추적하지 않는다.** 기록되지 않은 기간은 없었던 일로 취급한다. 이전에 검토했던 공백 감지·표시는 전부 제거한다.
14. **기간 조회는 화면마다 따로 만들지 말고 공통 패턴 하나로 만들어라.** "홍보물 관리", "매체 현황", "게시물", "타임라인" 네 화면 모두 "기간으로 조회" 체크박스 + 시작일·종료일을 갖는다. 켜면 현재 상태 스냅샷이 아니라 그 기간과 겹치는 모든 게시물의 이력 검색으로 전환되어야 한다 — 어느 화면에서 물어도 같은 방식으로 같은 답이 나와야 한다. 키워드 검색(업체명·내용·매체명)은 기간 조회 여부와 무관하게 항상 켜져 있다. "홍보물 관리"가 기본 화면이며, 모든 홍보물을 만료일 기준으로 정렬해 만료 건이 맨 앞에 오게 한다. "이력 조회"는 별도 탭으로 만들지 말고 타임라인에 통합하라 — 검색, 표/그래프 전환, 막대의 업체명 클릭 시 상세 이동을 타임라인 하나에서 처리한다.
15. **알람 문구는 최소화한다.** 조건·매체·업체명·날짜 등 판단에 필요한 값만 전달하고 부가 설명 문장을 넣지 않는다. 기준일(오늘 대신 쓰는 날짜)은 관리자가 화면에서 바꿀 수 있게 하라 — 상태·알람이 어떻게 나오는지 미리 점검하는 용도다.
16. **지도 성능 구조는 참조 코드를 그대로 가져와라.** pan은 React state가 아니라 `useRef` + 직접 DOM 조작, `Pin`은 `React.memo`, 휠 줌은 `useEffect`+ref로 stale closure를 피한다.
17. **배치도 이미지는 업로드하면 자동으로 지도 프레임 비율에 맞춰 중앙 크롭된다.** 배너 시스템의 센터맵 크롭 방식과 동일하게 처리하라.
18. **원본 이미지는 업로드하지 않는다.** 브라우저 canvas에서 WebP 두 벌(1600px / 400px)만 만들어 Storage에 올리고, 원본은 구글드라이브 링크만 텍스트로 저장한다. 프로토타입 `AddModal`의 `process()`가 기준 구현이다.
19. **시각 디자인은 프로토타입 그대로 유지한다.** 크림 배경 + 브라스 악센트 팔레트, 브랜드 마크·섹션 제목에 쓰인 콘덴스드 디스플레이 서체(Big Shoulders Display), 그리고 지도 핀의 사이니지 택(홀펀치 꼬리표) 모양은 의도적으로 다듬은 결과다. 임의로 기본 UI 라이브러리 스타일이나 다른 팔레트로 바꾸지 마라.

## 진행 방식

사양서 11장의 마일스톤 순서대로 간다. **1단계와 2단계 계획을 먼저 보여주고, 내 확인을 받은 뒤에 코드를 작성하라.** 이후 각 마일스톤이 끝날 때마다 무엇이 되었고 다음에 무엇을 할지 짧게 알려라.

Supabase 프로젝트는 2단계에서 필요하다. 그 시점에 무엇을 준비해야 하는지 알려주면 내가 만들겠다.

사양서 12장에 미확정 항목 7개(실제 규격, 자산 목록, 배치도, 철거 기록 주체, 이동 이력, 기존 게시물 종료일, 장기 기준일)가 있다. 그건 프로토타입 값으로 진행하되, 나중에 교체하기 쉽게 시드 데이터와 코드를 분리해 둘 것.

먼저 파일 3개를 읽고 1~2단계 계획을 보여줘.
